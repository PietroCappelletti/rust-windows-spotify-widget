/// Sets up the Windows system tray icon and its right-click menu
/// (Show/Hide, Quit).
pub struct TrayHandle;

impl TrayHandle {
  /// Creates the tray icon and menu.
  pub fn new() -> Self {
    todo!("build tray icon + menu with the `tray-icon` crate")
  }
}